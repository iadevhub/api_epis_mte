server.port=${PORT:8080}
spring.application.name=ca-api

# Chave de segurança para o endpoint /ca/reload
# Troque para uma senha forte antes de subir
ca.reload.key=${RELOAD_KEY:unomed2025}

# Scheduling habilitado
spring.task.scheduling.enabled=true
